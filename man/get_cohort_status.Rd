% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{get_cohort_status}
\alias{get_cohort_status}
\title{Get Cohort Status}
\usage{
get_cohort_status(data, pending_pattern = "Pending.*Decision$")
}
\arguments{
\item{data}{A MDUFA dataset with performance_metric, fy, and value columns}

\item{pending_pattern}{Regex pattern to identify the "pending" metric.
Defaults to "Pending.*Decision$" which matches MDUFA III/IV/V formats.}
}
\value{
Tibble with columns: fy (character), cohort_status ("open" or
"closed"), n_pending (integer count of pending submissions)
}
\description{
Determines whether a fiscal year cohort is "open" (submissions still pending)
or "closed" (all submissions have received decisions). A cohort is considered
closed when the "Number Pending" metric equals 0.
}
\examples{
library(dplyr)
mdufa4 |>
  filter(
    organization == "OHT7",
    program == "510(k)"
  ) |>
  get_cohort_status()
}
