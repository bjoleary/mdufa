% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{annotate_shutdowns}
\alias{annotate_shutdowns}
\title{Add Government Shutdown Markers}
\usage{
annotate_shutdowns(
  shutdowns = NULL,
  y_position = 0,
  color = "gray40",
  size = 2,
  scale_by_duration = FALSE
)
}
\arguments{
\item{shutdowns}{Shutdown data tibble (default: \link{gov_shutdowns}).
Can be filtered to show only significant shutdowns
(e.g., \code{gov_shutdowns |> dplyr::filter(duration_days >= 10)}).}

\item{y_position}{Y-axis position for markers}

\item{color}{Color for shutdown markers (default: "gray40")}

\item{size}{Size of markers (default: 2). Ignored if \code{scale_by_duration}
is TRUE.}

\item{scale_by_duration}{Logical, whether to scale marker size by shutdown
duration (default: FALSE). When TRUE, longer shutdowns have larger markers.}
}
\value{
A list of ggplot2 layers
}
\description{
Adds square markers on plots to indicate fiscal years affected by
government shutdowns that may have impacted FDA review timelines.
}
\examples{
\dontrun{
library(ggplot2)

# Add all shutdown markers at y = 0
ggplot(data, aes(x = fy, y = value)) +
  geom_point() +
  annotate_shutdowns(y_position = 0)

# Scale markers by shutdown duration
ggplot(data, aes(x = fy, y = value)) +
  geom_point() +
  annotate_shutdowns(y_position = 0, scale_by_duration = TRUE)

# Show only significant shutdowns (>= 10 days)
ggplot(data, aes(x = fy, y = value)) +
  geom_point() +
  annotate_shutdowns(
    shutdowns = gov_shutdowns |> dplyr::filter(duration_days >= 10),
    y_position = 0
  )
}
}
