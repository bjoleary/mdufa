% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cohort_closure.R
\name{plot_cohort_closure}
\alias{plot_cohort_closure}
\title{Plot Cohort Closure Percentage}
\usage{
plot_cohort_closure(
  data,
  show_phe = TRUE,
  phe_label_y = NA,
  color_by_period = TRUE,
  facet_ncol = 4L,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{data}{A data frame containing cohort closure metrics with columns:
\code{fy}, \code{org}, \code{percent_closed}, \code{status}.}

\item{show_phe}{Logical, whether to show COVID-19 Public Health Emergency
highlighting (default: TRUE).}

\item{phe_label_y}{Numeric, y-position for PHE label text. If NA (default),
position is calculated automatically from data.}

\item{color_by_period}{Logical, whether to color points by MDUFA period
(default: TRUE). Requires \code{report_mdufa_period} column in data.}

\item{facet_ncol}{Number of columns in facet grid (default: 4).}

\item{title}{Optional plot title. If NULL, a default title is generated.}

\item{subtitle}{Optional plot subtitle. If NULL, a default subtitle is used.}

\item{caption}{Plot caption. If NULL (default), auto-generates a caption
listing source reports with cutoff dates using
\code{\link{source_caption_with_dates}}. If FALSE or NA, no caption is
shown. If a character string, uses that value.}
}
\value{
A ggplot2 object that can be further customized with \code{+}.
}
\description{
Creates a visualization of cohort closure percentages over time.
Shows the percentage of submissions in each fiscal year cohort that
have received a final decision.
}
\details{
The plot displays cohort closure percentages using:
\itemize{
\item Points for each fiscal year/organization, with shape indicating status
\item Text labels showing the percentage value (100\% labels are hidden)
\item Colors by MDUFA reporting period when enabled
}

Unlike other plot functions, this one does not have a \code{fade_open_cohorts}
parameter because the cohort closure percentage IS the metric being
displayed.
}
\examples{
\dontrun{
# Basic cohort closure plot
cohort_status |>
  filter(program == "510(k)") |>
  plot_cohort_closure()

# With custom title
cohort_status |>
  filter(program == "510(k)") |>
  plot_cohort_closure(
    title = "510(k) Cohort Closure Status"
  )
}
}
